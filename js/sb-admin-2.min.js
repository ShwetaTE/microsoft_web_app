/*!
 * Start Bootstrap - SB Admin 2 v4.1.3 (https://startbootstrap.com/theme/sb-admin-2)
 * Copyright 2013-2021 Start Bootstrap
 * Licensed under MIT (https://github.com/StartBootstrap/startbootstrap-sb-admin-2/blob/master/LICENSE)
 */

!function(l) {
    "use strict";
    
    // Ensure sidebar is closed by default on page load
    l(document).ready(function() {
      l("body").addClass("sidebar-toggled");
      l(".sidebar").addClass("toggled");
    });
  
    // Toggle sidebar when clicking the sidebar toggle button
    l("#sidebarToggle, #sidebarToggleTop").on("click", function(e) {
      l("body").toggleClass("sidebar-toggled");
      l(".sidebar").toggleClass("toggled");
      // Close any open sidebar collapses when the sidebar is toggled
      l(".sidebar").hasClass("toggled") && l(".sidebar .collapse").collapse("hide");
    });
  
    // Handle sidebar collapse on window resize
    l(window).resize(function() {
      if (l(window).width() < 768) {
        l(".sidebar .collapse").collapse("hide");
      }
      
      // Automatically toggle the sidebar on smaller screens (below 480px)
      if (l(window).width() < 480 && !l(".sidebar").hasClass("toggled")) {
        l("body").addClass("sidebar-toggled");
        l(".sidebar").addClass("toggled");
        l(".sidebar .collapse").collapse("hide");
      }
    });
  
    // Prevent scrolling when hovering over the sidebar in fixed mode
    l("body.fixed-nav .sidebar").on("mousewheel DOMMouseScroll wheel", function(e) {
      var o;
      if (l(window).width() > 768) {
        o = (o = e.originalEvent).wheelDelta || -o.detail;
        this.scrollTop += 30 * (o < 0 ? 1 : -1);
        e.preventDefault();
      }
    });
  
    // Show scroll to top button after scrolling 100px
    l(document).on("scroll", function() {
      if (l(this).scrollTop() > 100) {
        l(".scroll-to-top").fadeIn();
      } else {
        l(".scroll-to-top").fadeOut();
      }
    });
  
    // Smooth scrolling to the top when the scroll-to-top button is clicked
    l(document).on("click", "a.scroll-to-top", function(e) {
      var o = l(this);
      l("html, body").stop().animate({
        scrollTop: l(o.attr("href")).offset().top
      }, 1000, "easeInOutExpo");
      e.preventDefault();
    });
  }(jQuery);
  